#ifndef CALCULATOR_H
#define CALCULATOR_H

#include <iostream>
#include <vector>
#include <unordered_map>
#include <QString>
#include <string>
#include <string_view>
#include <algorithm>
#include <stack>
#include <memory>
#include <cmath>
#include <cctype>
#include <regex>

struct Coords{
    double x;
    double y;
};


/*
int main() {
    for (;;) {
        std::cout << "<calc> ";
        std::string input;
        std::getline(std::cin, input);
        Calculator::getInstance(input);
    }
    return 0;
}
*/

#endif // CALCULATOR_H
